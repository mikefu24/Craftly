{
  "name": "craftly",
  "version": "1.0.0",
  "description": "Craftly — AI 应用构建器（类 Lovable）",
  "private": true,
  "scripts": {
    "cap:android": "cap add android && cap sync android",
    "cap:ios": "cap add ios && cap sync ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/android": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  }
}
